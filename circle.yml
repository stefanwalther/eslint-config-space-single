machine:
  node:
    version: 8
  environment:
      PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - nvm use 8.6.0; yarn run test
    - nvm use latest; yarn run test-coverage
  post:
    - bash <(curl -s https://codecov.io/bash)
